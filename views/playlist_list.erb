<% if @playlists.any? %>
<h1>Your Playlists: </h1>

<ul class='row'>
  <% @playlists.each do |playlist| %>
  <%= erb(:_playlist_card, :locals => {:playlist => playlist}) %>
  <% end %>
</ul>

<% end %>

<%= erb(:_playlist_form) %>
